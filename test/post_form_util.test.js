import Mocha from 'mocha';
import PostFormUtil from '../client/src/components/posts/util/functions/post_form_util.js';
const {} = PostFormUtil;

describe('Post Form Util', function() {
  
  describe('#handlePostId', function() {
    it('returns the postId for a Liked Repost', function() {

    });

    it('returns the postId for a Liked Post', function() {

    });

    it('returns the postId for all other posts', function() {

    })
  });
});